// Exemplo para app/(tabs)/registros.tsx
import { Text, View } from 'react-native';

export default function RegistrosScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Tela de Registros</Text>
    </View>
  );
}